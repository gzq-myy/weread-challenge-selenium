{
  "name": "weread-selenium-cli",
  "version": "0.18.0",
  "description": "Patchright automation tool for WeRead challenge",
  "main": "src/weread-challenge.js",
  "bin": {
    "weread-challenge": "src/weread-challenge.js",
    "weread-selenium-cli": "src/weread-challenge.js"
  },
  "files": [
    "src",
    "README.md",
    "README-dev.md"
  ],
  "license": "MIT",
  "scripts": {
    "start": "DEFAULT_BOOK_URL=https://weread.qq.com/web/reader/276323e0813ab90a5g0144d7 node src/weread-challenge.js run",
    "dev": "WEREAD_KEYWORDS=三体,明朝 WEREAD_HEADLESS=false node src/weread-challenge.js run",
    "debug": "DEBUG=true WEREAD_KEYWORDS=三体,明朝 WEREAD_HEADLESS=false node src/weread-challenge.js run",
    "test:runtime": "node scripts/runtime-smoke.js all",
    "test:runtime:npx": "node scripts/runtime-smoke.js npx",
    "test:runtime:docker": "node scripts/runtime-smoke.js docker",
    "link:global": "npm link",
    "unlink:global": "npm unlink -g weread-selenium-cli",
    "docker:image:help": "node scripts/docker-image.js help",
    "docker:image:check": "node scripts/docker-image.js check",
    "docker:image:build": "node scripts/docker-image.js build",
    "docker:image:save": "node scripts/docker-image.js save",
    "docker:image:save:amd64": "node scripts/docker-image.js save --platform linux/amd64",
    "docker:image:push": "node scripts/docker-image.js push",
    "docker:image:push:dev": "node scripts/docker-image.js push --tag dev",
    "publish:npm:check": "node -e \"const p=require('./package.json');const miss=['name','version'].filter(k=>!p[k]);if(miss.length){console.error('缺少字段: '+miss.join(','));process.exit(1);}if(!/^\\d+\\.\\d+\\.\\d+(?:-[0-9A-Za-z.-]+)?(?:\\+[0-9A-Za-z.-]+)?$/.test(p.version)){console.error('version 不是有效 semver: '+p.version);process.exit(1);}console.log('发布校验通过: '+p.name+'@'+p.version);\"",
    "publish:npm:dry-run": "npm run publish:npm:check && npm publish --access public --dry-run",
    "publish:npm": "npm run publish:npm:check && npm publish --access public"
  },
  "dependencies": {
    "dotenv": "^17.2.3",
    "jsqr": "^1.4.0",
    "patchright": "^1.60.0",
    "pngjs": "^7.0.0",
    "qrcode-terminal": "^0.12.0"
  }
}
